---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: dualdeploymentoperators.dual-deployment-operator.cc.sap
spec:
  group: dual-deployment-operator.cc.sap
  names:
    kind: DualDeploymentOperator
    listKind: DualDeploymentOperatorList
    plural: dualdeploymentoperators
    shortNames:
    - ddo
    singular: dualdeploymentoperator
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DualDeploymentOperatorSpec defines the desired state of DualDeploymentOperator.
            properties:
              deletionPolicy:
                default:
                  crds: Retain
                properties:
                  crds:
                    default: Retain
                    enum:
                    - Retain
                    - Delete
                    type: string
                type: object
              remoteKubeconfig:
                properties:
                  key:
                    minLength: 1
                    type: string
                  secretName:
                    minLength: 1
                    type: string
                required:
                - key
                - secretName
                type: object
              source:
                description: Source is a discriminated union — exactly one of Helm
                  or Kustomize.
                properties:
                  helm:
                    properties:
                      hostValues:
                        x-kubernetes-preserve-unknown-fields: true
                      name:
                        minLength: 1
                        type: string
                      remoteValues:
                        x-kubernetes-preserve-unknown-fields: true
                      repo:
                        minLength: 1
                        type: string
                      values:
                        x-kubernetes-preserve-unknown-fields: true
                      version:
                        minLength: 1
                        type: string
                    required:
                    - name
                    - repo
                    - version
                    type: object
                  kustomize:
                    description: KustomizeSource references a kustomize root plus
                      its two overlay subpaths.
                    properties:
                      hostPath:
                        minLength: 1
                        type: string
                      remotePath:
                        minLength: 1
                        type: string
                      url:
                        minLength: 1
                        type: string
                    required:
                    - hostPath
                    - remotePath
                    - url
                    type: object
                    x-kubernetes-validations:
                    - message: kustomize url must include a pinned ref= query parameter
                      rule: self.url.matches('.*[?&]ref=.+')
                type: object
                x-kubernetes-validations:
                - message: exactly one of source.helm or source.kustomize must be
                    set
                  rule: has(self.helm) != has(self.kustomize)
              transformations:
                items:
                  description: Transformation is a discriminated union — exactly one
                    field set per entry.
                  properties:
                    filterKinds:
                      properties:
                        kinds:
                          items:
                            type: string
                          minItems: 1
                          type: array
                        source:
                          type: string
                      required:
                      - kinds
                      type: object
                    packageWebhookConfigsForInjector:
                      properties:
                        configMapName:
                          minLength: 1
                          type: string
                        dataKey:
                          type: string
                      required:
                      - configMapName
                      type: object
                    patch:
                      description: PatchSpec is a Kubernetes-native patch (strategic-merge
                        XOR JSON Patch) with a target selector.
                      properties:
                        jsonPatch:
                          items:
                            description: JSONPatchOp is one RFC 6902 operation.
                            properties:
                              from:
                                type: string
                              op:
                                enum:
                                - add
                                - remove
                                - replace
                                - move
                                - copy
                                - test
                                type: string
                              path:
                                minLength: 1
                                type: string
                              value:
                                x-kubernetes-preserve-unknown-fields: true
                            required:
                            - op
                            - path
                            type: object
                          type: array
                        strategicMerge:
                          x-kubernetes-preserve-unknown-fields: true
                        target:
                          properties:
                            kind:
                              type: string
                            name:
                              type: string
                            origin:
                              type: string
                          type: object
                      required:
                      - target
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of patch.strategicMerge or patch.jsonPatch
                          must be set
                        rule: has(self.strategicMerge) != has(self.jsonPatch)
                    rewriteWebhookURL:
                      properties:
                        targetKinds:
                          items:
                            type: string
                          type: array
                        urlPrefix:
                          minLength: 1
                          type: string
                      required:
                      - urlPrefix
                      type: object
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one transformation type must be set per entry
                    rule: '(has(self.patch) ? 1 : 0) + (has(self.rewriteWebhookURL)
                      ? 1 : 0) + (has(self.filterKinds) ? 1 : 0) + (has(self.packageWebhookConfigsForInjector)
                      ? 1 : 0) == 1'
                type: array
            required:
            - remoteKubeconfig
            - source
            type: object
          status:
            description: DualDeploymentOperatorStatus defines the observed state of
              DualDeploymentOperator.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              hostResources:
                items:
                  properties:
                    apiVersion:
                      type: string
                    health:
                      enum:
                      - Healthy
                      - Progressing
                      - Degraded
                      - Unknown
                      type: string
                    kind:
                      type: string
                    lastApplied:
                      format: date-time
                      type: string
                    message:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - apiVersion
                  - health
                  - kind
                  - name
                  type: object
                type: array
              lastReconcile:
                format: date-time
                type: string
              remoteResources:
                items:
                  properties:
                    apiVersion:
                      type: string
                    health:
                      enum:
                      - Healthy
                      - Progressing
                      - Degraded
                      - Unknown
                      type: string
                    kind:
                      type: string
                    lastApplied:
                      format: date-time
                      type: string
                    message:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - apiVersion
                  - health
                  - kind
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
