apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: RoleBinding
{{- else }}
kind: ClusterRoleBinding
{{- end }}
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "dual-deployment-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "dual-deployment-operator.resourceName" (dict "suffix" "manager-rolebinding" "context" $) }}
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
{{- if .Values.rbac.namespaced }}
  kind: Role
{{- else }}
  kind: ClusterRole
{{- end }}
  name: {{ include "dual-deployment-operator.resourceName" (dict "suffix" "dual-deployment-operator" "context" $) }}
subjects:
- kind: ServiceAccount
  name: {{ include "dual-deployment-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
